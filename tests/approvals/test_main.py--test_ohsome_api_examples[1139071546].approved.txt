geometry:polygon and building=* and building!=no and area:(1E6..)

((status_geom_type).geom_type = 'Polygon' OR (status_geom_type).geom_type = 'MultiPolygon') AND tags ? $1 AND NOT tags @> $2 AND area >= $3

('building', '{"building": "no"}', 1000000.0)

[<Record count=14>]
